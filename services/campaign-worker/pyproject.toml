[project]
name = "campaign-worker"
version = "0.1.0"
description = "Sigulon outbound campaign worker: Redis queue -> Plivo dial -> lifecycle convergence"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cryptography==46.0.3",
    "redis==8.1.0",
    "pymongo==4.18.1",
    "httpx==0.28.1",
]
