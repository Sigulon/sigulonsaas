# Redis configuration for Sigulon local & cluster environments.
# The campaign queue (sigulon:campaign:queue) and retry ZSET share this
# instance with caches — eviction must NEVER drop queue keys.
maxmemory 2gb
maxmemory-policy noeviction
save 60 1000
appendonly yes
appendfsync everysec
tcp-keepalive 60
timeout 300
